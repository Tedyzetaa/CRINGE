# Dependências do Backend (FastAPI, uvicorn, httpx, etc.)
fastapi
uvicorn[standard]
sqlalchemy
pydantic
httpx

# Dependências do Frontend (Streamlit)
streamlit
requests

# Dependências de Banco de Dados
# O SQLAlchemy já está lá, mas precisamos do driver:
psycopg2-binary  # Driver para PostgreSQL (recomendado para produção/Render)

# Dependências de Segurança/Configuração
python-dotenv